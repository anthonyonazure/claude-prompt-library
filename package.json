{
  "name": "claude-prompt-library",
  "version": "0.1.0",
  "description": "Production-ready Claude prompts with structured outputs and eval harness",
  "type": "module",
  "scripts": {
    "run": "tsx src/cli.ts",
    "eval": "tsx src/eval.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.91.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
